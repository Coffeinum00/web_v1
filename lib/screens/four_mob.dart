import 'package:flutter/material.dart';

class FourMob extends StatelessWidget {
  const FourMob({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
